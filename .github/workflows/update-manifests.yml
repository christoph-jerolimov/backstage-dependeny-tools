name: Update manifests

on:
  workflow_dispatch:
  schedule:
    # Every morning at 07:17 UTC (09:17 CEST / 08:17 CET)
    - cron: '17 7 * * *'

permissions:
  contents: write

concurrency:
  group: update-manifests
  cancel-in-progress: false

jobs:
  update-manifests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Checkout backstage/versions
        uses: actions/checkout@v4
        with:
          repository: backstage/versions
          path: .clones/versions

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Copy manifests
        run: npm run copy-manifests -- .clones/versions

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add manifests
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update manifests from backstage/versions"
            git push origin main
          fi
